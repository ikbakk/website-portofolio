---
const { row } = Astro.props;
---
<a class:list={['contact-row', !row.enabled && 'is-disabled']} href={row.enabled ? row.href : undefined} aria-disabled={!row.enabled ? 'true' : undefined}>
  <span>{row.label}</span>
  <strong>{row.value}</strong>
  <small>{row.note}</small>
</a>
