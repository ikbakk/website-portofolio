---
/**
 * Colophon. The 5-row footer block at the very bottom of the
 * page. Renders the live clock slot as .js-clock-slot so the
 * clock module fills it on the client.
 */
import type { ColophonData } from "../data/colophon";

interface Props {
  data: ColophonData;
}

const { data } = Astro.props;
---

<footer class="colophon" data-od-id="colophon" role="contentinfo">
  {data.rows.map((row) => (
    <div class="colophon-row">
      <span>{row.label}</span>
      {row.variant === "clock" ? (
        <span class="colophon-clock js-clock-slot" data-od-id="colophon-clock">
          {row.value}
        </span>
      ) : (
        <span>{row.value}</span>
      )}
    </div>
  ))}
</footer>
