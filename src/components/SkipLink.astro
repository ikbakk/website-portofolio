---
/**
 * SkipLink. The first child of <body>. Targets the page main.
 * Becomes visible on focus.
 */
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a class="skip-link" href={href}>{label}</a>
